replicaCount: 1

podAnnotations:
  kubernetes.azure.com/set-kube-service-host-fqdn: "true"

flags:
  logLevel: info
  logFormat: json

nodeSelector:
  workload: system

resources:
  requests:
    cpu: 100m
    memory: 128Mi
  limits:
    cpu: 500m
    memory: 512Mi
